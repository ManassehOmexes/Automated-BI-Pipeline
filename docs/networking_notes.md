# Networks

- IP-Adressen (localhost = 127.0.0.1)
- DNS (Domain → IP)
- Ports (80=HTTP, 443=HTTPS, 5432=PostgreSQL)
- HTTP vs HTTPS (immer HTTPS für sensible Daten)
- API-Calls mit curl
- localhost vs 0.0.0.0 (wichtig für Docker)
